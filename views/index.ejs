
<!-- Include Header (Narik dari _header.ejs)-->
    <%- include('include/_header') %>
<!-- /include header -->

    <!-- Main Site -->
    <main id="site-main">
        <div class="container">
            <div class="box-nav d-flex justify-between">
                <a href="/add-user" class="border-shadow">
                    <span class="text-gradient">New User <i class="fa-solid fa-user"></i></span>
                </a>
            </div>

            <form action="/" method="POST">
                <table class="table">
                    <thead class="thead-dark">
                        <tr>
                            <th>ID</th>
                            <th>Nama Lengkap</th>
                            <th>Alamat</th>
                            <th>Email</th>
                            <th>No.Whatsapp</th>
                            <th>Jumlah Tiket</th>
                            <th>Section</th>
                            <th>Payment</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Menampilkan isi tabel (Narik dari _show.ejs) -->
                        <%- include("include/_show") %>
                    </tbody>
                </table>
            </form>
        </div>
    </main>

<!-- Include footer Narik dari _footer.ejs -->
    <%- include('include/_footer') %>
<!-- /include footer -->